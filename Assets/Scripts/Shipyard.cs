using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shipyard : MonoBehaviour
{
    //TODO this should be a crafter, with recipes to create certain part of the boat (some need to be create before others, such as the body of the boat)
    //TODO everytime we craft a part, that part needs to be put on a fixed position on the boat

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
